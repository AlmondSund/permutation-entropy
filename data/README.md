# Data Directory

Place raw seismic data in `data/raw/`. Intermediate processing outputs can live in `data/interim/`, and cleaned datasets in `data/processed/`. This folder is git-ignored by default.
